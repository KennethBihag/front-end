{
    "tasks": [
        {
            "label": "Go Live  Chrome",
            "type": "shell",
            "command": "live-server --browser=chrome"
        },
        {
            "label": "Start Server",
            "type": "shell",
            "command": "pushd server && npm start"
        }
    ]
}